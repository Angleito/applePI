install_agent_hooks = ["pi"]

provider = "pi"
option_defaults = { model = "deepseek-v4-flash" }
min_active_sessions = 0
max_active_sessions = 8
wake_mode = "fresh"

[env]
PI_SUBAGENT_ENABLED = "1"
PI_SUBAGENT_ALLOWED = "scout"
PI_SUBAGENT_MAX_RUNNING = "3"