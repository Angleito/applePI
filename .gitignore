# ApplePI machine-local state and artifacts
.env
*.db
/tests/work/
node_modules/
.applepi/

# Local editor/OS noise
*.swp
.DS_Store

# Gas City (phase0) history lives on the archived branch `phase0-pi-codex`
# and in docs/ROADMAP.md; the phase0 harness itself is no longer maintained.
